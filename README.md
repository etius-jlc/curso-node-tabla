# Notas:
```
Este es mi primer programa en Node
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base del cálculo (tabla de...)
                                                [number] [required] [default: 1]
  -l, --listar   Indica si lista o no la tabla        [boolean] [default: false]
  -h, --hasta    Indica el límite del factor              [number] [default: 10]
```